stages:
  - docker-build
  - deploy


docker-build:
  stage: docker-build
  image: docker:20.10.16
  services:
    - docker:dind
  before_script:
    - echo $CI_REGISTRY_PASSWORD |docker login -u $CI_REGISTRY_USER $CI_REGISTRY --password-stdin
  script:
    - docker build --pull -t $CI_REGISTRY_IMAGE .
    - docker push $CI_REGISTRY_IMAGE
    - echo "Registry image:" $CI_REGISTRY_IMAGE
  only:
    - master

# deploy:
#   stage: deploy
#   image: linuxserver/openssh-server
#   before_script:
#     - echo "deploying app"
#   script:
#     - chmod 400 $LOGIN_FILE
#     - ssh -o StrictHostKeyChecking=no -i $LOGIN_FILE ubuntu@$PASSWD "echo $CI_REGISTRY_PASSWORD |docker login -u $CI_REGISTRY_USER $CI_REGISTRY --password-stdin; sudo docker pull registry.gitlab.com/do-copypaste/copypaste_node:latest ; cd server; docker-compose up -d"
                                                                                                                                                                                      



